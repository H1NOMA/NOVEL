{
  "name": "novel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Прототип визуальной новеллы: движко-независимое ядро + web-раннер",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
